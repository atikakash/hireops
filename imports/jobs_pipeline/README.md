Imported job and pipeline source files.

Structure:

- `flutter/`
  Contains the provided Dart files copied from `c:\Users\Tanzim Rashid\Downloads\files (3)`.
- `backend/`
  Contains the provided Node/Express files copied from the same source folder.

These files were added in an isolated import area so the current Flutter app is not overwritten.
The existing app already has its own `jobs` and `pipeline` implementation under `lib/features/`.
